export async function submitLeadStub(data){
  // UI-only stub. Replace with Airtable / backend integration.
  console.log('submitLeadStub', data)
  return new Promise((res) => setTimeout(() => res({ ok: true }), 700))
}